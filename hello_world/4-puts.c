#include <stdio.h>
int main(void)

/**
 * main - Entry point
 *
 * Description: This program prints a sentence using the `puts` function.
 * It demonstrates basic output in C.
 *
 * Return: Always 0 (Success)
 */
{
	puts("\"Programming is like building a multilingual puzzle/n");
	return (0);
}
