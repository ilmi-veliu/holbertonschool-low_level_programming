#include "main.h"
/**
 * _abs - Calcule la valeur absolue d'un entier
 * @r: L'entier à traiter
 *
 * Return: La valeur absolue de r
 */
int _abs(int r)
{
	if (r < 0)
	return (-r);

	else
	return (r);
}
